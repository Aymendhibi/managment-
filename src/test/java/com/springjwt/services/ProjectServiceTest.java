package com.springjwt.services;

import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@RunWith(SpringRunner.class)
@SpringBootTest
public  class ProjectServiceTest {

/*
    @Autowired
    private  IProjectService iProjectService;

    @Test
    public  void  shouldsaveprojetwithSuccess(){

       ProjectDTO expectedProject = ProjectDTO.builder()
               .title("title test")
               .createdDate("date test")
               .id(1)
               .build();
        ProjectDTO savedProject=  iProjectService.addprojectt(expectedProject);
        assertNotNull(savedProject);
        assertNotNull(savedProject.getId());
        assertEquals(expectedProject.getTitle(),savedProject.getTitle());
        assertEquals(expectedProject.getCreatedDate(),savedProject.getCreatedDate());



    }*/

}
